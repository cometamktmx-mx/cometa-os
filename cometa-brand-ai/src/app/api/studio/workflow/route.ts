import { NextResponse } from "next/server"; import { transitionStudioPiece } from "@/lib/studio/workflow";
export async function POST(request: Request){try{const body=await request.json() as {pieceId?:string;action?:string};if(!body.pieceId||!body.action||!["start","ready"].includes(body.action))return NextResponse.json({ok:false,error:"WORKFLOW_ACTION_INVALID"},{status:400});return NextResponse.json({ok:true,result:await transitionStudioPiece(body.pieceId,body.action)});}catch(error){const code=error instanceof Error?error.message:"WORKFLOW_FAILED";return NextResponse.json({ok:false,error:code},{status:code.includes("NOT_FOUND")?404:403});}}
