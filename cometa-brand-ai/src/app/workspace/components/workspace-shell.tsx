"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import type { ReactNode } from "react";
type NavItem = { label: string; href?: string; icon: string };
function Nav({ items }: { items: NavItem[] }) { const pathname = usePathname(); const enabled = items.map((item) => item.href ? item : item.label === "Contenido" ? { ...item, href: "/workspace/content" } : item.label === "Aprobaciones" ? { ...item, href: "/workspace/approvals" } : item); return <nav className="space-y-1">{enabled.map((item) => item.href ? <Link key={item.label} href={item.href} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition ${pathname === item.href || (item.href !== "/workspace" && pathname.startsWith(`${item.href}/`)) ? "bg-blue-600 text-white" : "text-slate-400 hover:bg-white/[.06] hover:text-white"}`}><span className="grid h-7 w-7 place-items-center rounded-lg bg-white/[.08]">{item.icon}</span>{item.label}</Link> : <div key={item.label} className="flex items-center justify-between rounded-xl px-3 py-2.5 text-sm text-slate-600"><span className="flex items-center gap-3"><span className="grid h-7 w-7 place-items-center rounded-lg border border-white/[.06]">{item.icon}</span>{item.label}</span><span className="text-[9px] uppercase tracking-wider">Próximamente</span></div>)}</nav>; }
export function WorkspaceShell({ children, userName, isAdmin = true }: { children: ReactNode; userName?: string | null; isAdmin?: boolean }) { return <div className="min-h-screen bg-[#07111f] text-slate-100"><aside className="fixed inset-y-0 left-0 z-20 hidden w-64 border-r border-white/[.07] bg-[#081525] lg:flex lg:flex-col"><div className="px-5 pb-6 pt-7"><div className="text-lg font-semibold tracking-[.28em]">COMETA</div><div className="mt-1 text-[10px] uppercase tracking-[.25em] text-cyan-300">Admin Workspace</div></div><div className="flex-1 space-y-7 px-3"><div><p className="mb-2 px-3 text-[10px] uppercase tracking-[.2em] text-slate-600">Operación</p><Nav items={[{ label: "Workspace", href: "/workspace", icon: "⌂" }, { label: "Marcas", href: "/workspace/brands", icon: "◈" }]} /><Nav items={[{ label: "Calendarios", href: "/workspace/calendars", icon: "□" }, { label: "Contenido", icon: "✦" }, { label: "Aprobaciones", icon: "✓" }]} /></div><div><p className="mb-2 px-3 text-[10px] uppercase tracking-[.2em] text-slate-600">Inteligencia</p><Nav items={[{ label: "Knowledge", icon: "⌁" }, { label: "Learning", icon: "↗" }, { label: "Reportes", icon: "▤" }]} /></div><div><p className="mb-2 px-3 text-[10px] uppercase tracking-[.2em] text-slate-600">Sistema</p><Nav items={[...(isAdmin ? [{ label: "Accesos", href: "/workspace/access", icon: "♙" }] : []), { label: "Configuración", icon: "⚙" }]} /></div></div><div className="border-t border-white/[.07] p-4"><div className="rounded-xl bg-white/[.04] p-3"><p className="text-xs font-medium text-white">{userName || "Equipo Cometa"}</p><p className="mt-1 text-[11px] text-slate-500">{isAdmin ? "Administrador" : "Acceso a marcas"}</p></div></div></aside><main className="min-h-screen lg:pl-64"><header className="sticky top-0 z-10 border-b border-white/[.07] bg-[#07111f]/90 px-5 py-4 backdrop-blur-xl sm:px-8"><div className="mx-auto flex max-w-[1500px] items-center justify-between"><div><p className="text-[10px] uppercase tracking-[.22em] text-cyan-300">COMETA / ADMIN</p><p className="mt-1 text-sm text-slate-400">Centro operativo de Cometa</p></div>{isAdmin && <div className="flex items-center gap-3"><Link href="/workspace/brands" className="hidden rounded-lg border border-white/10 px-3 py-2 text-xs text-slate-300 hover:text-white sm:block">Buscar marca</Link><Link href="/workspace/brands/new" className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-semibold text-white hover:bg-blue-500">+ Nueva marca</Link></div>}</div></header><div className="mx-auto max-w-[1500px] px-5 py-8 sm:px-8">{children}</div></main></div>; }
