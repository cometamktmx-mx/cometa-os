import Link from "next/link";
import { OsIcon, PageHeader, Section, StatusBadge } from "../components/os-primitives";
const sections = [["Perfil del negocio", "La información que identifica y contextualiza tu negocio.", "report"], ["Usuarios y acceso", "Gestiona quién puede entrar a Cometa OS.", "link"], ["Sales AI", "Modo, atención y configuración comercial.", "sales"], ["Notificaciones", "Define cuándo quieres enterarte de lo importante.", "alert"], ["Conexiones", "WhatsApp Business y Cometa POS.", "link"], ["Preferencias de marca", "Ajustes de presentación y comunicación.", "spark"]] as const;
export default async function SettingsPage({ params }: { params: Promise<{ brandSlug: string }> }) { const { brandSlug } = await params; const base = `/brand/${encodeURIComponent(brandSlug)}/os`; return <div className="space-y-6"><PageHeader eyebrow="CONFIGURACIÓN" title="Tu espacio de trabajo" description="Gestiona la experiencia de tu negocio dentro de Cometa OS." /><Section title="Preferencias del negocio" description="Una vista clara de lo que puedes configurar hoy."><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{sections.map(([title, summary, icon]) => <article key={title} className="os-card min-h-[145px] transition hover:border-blue-200"><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-100 text-slate-700"><OsIcon name={icon} className="h-5 w-5" /></div><StatusBadge label="Disponible" /></div><h2 className="mt-4 text-sm font-semibold">{title}</h2><p className="mt-1 text-xs leading-5 text-[var(--os-text-muted)]">{summary}</p>{title === "Conexiones" ? <Link href={`${base}/connections`} className="mt-3 inline-flex text-xs font-semibold text-blue-700">Ver conexiones →</Link> : <p className="mt-3 text-xs text-slate-400">La configuración detallada se mantiene en su superficie actual.</p>}</article>)}</div></Section><div className="os-card-subtle"><p className="os-label">SEGURIDAD</p><p className="mt-3 text-sm font-semibold">La configuración interna de Cometa no aparece aquí.</p><p className="mt-1 text-xs leading-5 text-[var(--os-text-muted)]">Secretos, modelos, prompts y ajustes técnicos permanecen protegidos.</p></div></div>; }
