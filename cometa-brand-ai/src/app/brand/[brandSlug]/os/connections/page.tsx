import Link from "next/link";
import { getPassivePosProductAvailability } from "@/lib/pos/access";
import { OsIcon, PageHeader, Section, StatusBadge } from "../components/os-primitives";
export default async function ConnectionsPage({ params }: { params: Promise<{ brandSlug: string }> }) { const { brandSlug } = await params; const pos = await getPassivePosProductAvailability(brandSlug); const base = `/brand/${encodeURIComponent(brandSlug)}/os`; return <div className="space-y-6"><PageHeader eyebrow="CONEXIONES" title="Fuentes que mantienen a Cometa informado" description="Conecta las herramientas que permiten entender y operar mejor tu negocio." /><Section title="Tus conexiones" description="Sólo mostramos integraciones disponibles para esta marca."><div className="grid gap-4 md:grid-cols-2"><article className="os-card-primary"><div className="flex items-start justify-between gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-50 text-emerald-700"><OsIcon name="link" className="h-5 w-5" /></div><StatusBadge label="Disponible" /></div><h2 className="mt-5 text-base font-semibold">WhatsApp Business</h2><p className="mt-1 text-sm leading-5 text-[var(--os-text-muted)]">Activa conversaciones, captura de leads y seguimientos con Sales AI.</p><Link href={`${base}/sales`} className="mt-5 inline-flex text-sm font-semibold text-blue-700">Gestionar conexión →</Link></article><article className="os-card-primary"><div className="flex items-start justify-between gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-blue-50 text-blue-700"><OsIcon name="report" className="h-5 w-5" /></div><StatusBadge label={pos.available ? "Conectado" : "Disponible"} tone={pos.available ? "positive" : "neutral"} /></div><h2 className="mt-5 text-base font-semibold">Cometa POS</h2><p className="mt-1 text-sm leading-5 text-[var(--os-text-muted)]">{pos.available ? "Ventas, productos, inventario y clientes enriquecen Cometa Intelligence." : "Conecta Cometa POS para enriquecer Intelligence con datos operativos."}</p>{pos.available ? <Link href={`/brand/${encodeURIComponent(brandSlug)}/pos`} className="mt-5 inline-flex text-sm font-semibold text-blue-700">Abrir Cometa POS ↗</Link> : <span className="mt-5 inline-flex text-sm font-semibold text-slate-500">Disponible cuando lo necesites</span>}</article></div></Section><div className="os-card-subtle flex items-start gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-slate-100 text-slate-600"><OsIcon name="check" className="h-4 w-4" /></div><div><p className="text-sm font-semibold">Tus datos permanecen separados</p><p className="mt-1 text-xs leading-5 text-[var(--os-text-muted)]">Cada conexión se muestra dentro del contexto seguro de tu negocio.</p></div></div></div>; }
